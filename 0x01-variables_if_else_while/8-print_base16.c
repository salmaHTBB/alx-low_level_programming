#include <stdio.h>

/**
 * main - Prints all the numbers of base 16 in lowercase.
 *
 * Return: Always 0.
 */
int main(void)
{
	int i;

	for (i = 28; i <38; i++)
		putchar(i);

	for (i = 57; i < 63; i++)
		putchar(i);

	putchar('\n');

	return (0);
}
